<h1>Editando a tarefa {{task.title}}</h1>
<form action="/tasks/add" method="POST" class="form-control">
<input type="hidden" name="id" value="{{ task.id }}">
<div>
    <label for="title" >Titulo</label>
    <input type="text" name="title" placeholder="o que voce vai fazer?" value="{{task.title}}">
</div>

<div>
    <label for="title" >Titulo</label>
    <textarea name="description" placeholder="Descreva a tarefa" id="" cols="30" rows="10">{{task.description}}</textarea>
</div>

<div>
    <input type="submit" value="Editar">
</div>
</form>